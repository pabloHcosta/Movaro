{
  "name": "mudavi-api-edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types && tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "types": "wrangler types"
  },
  "devDependencies": {
    "typescript": "latest",
    "wrangler": "latest"
  }
}
